{% extends "base.html.twig" %}

{% block body %}
<div class="container-fluid container-sm mt-4">
    <div class="col-sm-6 mx-auto">
        <h1 class="display-6 display-5-md">{{ form_title }}</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success">Valider</button>
        <a href="{{ path("app_categories") }}" class="btn btn-warning">Annuler</a>
        {{ form_end(form) }}

    </div>
</div>
{% endblock %}